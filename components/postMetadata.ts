export interface postMetadata {
    title: string;
    subtitle: string;
    date: string;
    tags: string[];
    toc: string;
    slug: string;
    folder: string;
    series: number;
    cover: string;
}